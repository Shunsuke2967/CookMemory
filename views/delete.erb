<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://unpkg.com/ress/dist/ress.min.css">
    <link rel="stylesheet" href="/style.css">
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <title>CookRecipe_delete</title>
  </head>
  <body>

    <div class="wrapper header">
      <h1 class="home-content"><a href= "/">CookRecipe</a></h1>
      <div class="nav-bar">
        <a href= "/">HOME</a>
      </div>
    </div>

    <div class="wrapper">
      <h2 class="delete-h2">この投稿を本当に削除しますか？</h2>
      <div class="table_content">
        <table>
          <% if @content.title.present? %>
            <tr><th><%= @content.title %></th></tr>
          <% else %>
            <tr><th>タイトルがありません</th></tr>
          <% end %>
          <% r = Random.new %>
          <% salt = Digest::SHA256.hexdigest(r.bytes(20)) %>
          <tr><td><img src="/img/<%= @content.imgfile %>?<%= salt %>"/></td></tr>
          <tr><th>作り方</th></tr>
          <tr><td><div id="input_content"><%= @content.post_content %></td></tr>
        </table>
      </div>
      <div class="delete-button">
        <form method="post" action="/delete/<%= @content.id %>">
        <button type="submit">削除を実行する</button>
        </form>
      </div>
    </div>
  </body>
</html>